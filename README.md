# Goal of this repository

The main goal of this repository is to try basic ML examples using popular frameworks

## Preparing venv

To prepare venv use these commands:

```shell
uv venv --python 3.14 --prompt ml_experiments
source .venv/bin/activate && uv sync
```
